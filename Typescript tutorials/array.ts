const superHero: string[] = [];

// we can also write the above syntax in this manner 

const cricketer: Array<String> = [];
// Both the way of writing the code is best, no good  or bad, all is good.

superHero.push("Yash");

type User ={
    name:string
    isActive:boolean
}
const allUsers: User[]=[];
 
allUsers.push({name:"Yash",isActive:true})


export {}